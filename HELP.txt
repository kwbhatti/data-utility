gpg -ab data-utility-0.0.1-SNAPSHOT.jar
gpg --verify data-utility-0.0.1-SNAPSHOT.jar
mvn nexus-staging:release
mvn nexus-staging:drop

will not execute the test (only package)
mvn clean install

will run the tests but will not create asc files for publishing
mvn -Pcore -Ptest -DtestngFile=testng.xml clean install

will run the test and generate asc files for publishing
mvn -Pcore -Ptest -Ppublish -DtestngFile=testng.xml clean install